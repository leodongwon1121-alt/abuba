# 디자인 규칙 (어부바)

톤: 수산물 커머스의 신뢰감 있는 색감(피쉬세일 참고) + 배달의민족의 카드형 리스트·하단 고정 탭바·강조 배지 레이아웃. 부산 어업 종사자와 소비자 모두가 쓰는 서비스이므로 장난스러움보다 "믿을 수 있고 정돈된" 인상을 우선한다.

## 금지 목록
- 보라/인디고 그라데이션, `bg-indigo-*`, `bg-purple-*` 클래스
- Inter 폰트
- 아이콘 카드 3개를 가로로 나란히 배치하는 "feature grid" 패턴
- "Get Started" 류 파란 CTA 버튼
- raw hex 코드를 컴포넌트에 직접 쓰는 것 — 반드시 CSS 변수 사용
- (추가) 보라·파랑 그라데이션 배경, 이모지를 주요 시각 요소로 사용, 과도하게 둥근 모서리, 진한 드롭섀도우
- (추가) 배달의민족 특유의 민트/보라/빨강 색감 — 레이아웃 패턴만 참고하고 색은 가져오지 않음

## 강제 규칙
- **폰트**: 디스플레이/본문 Pretendard (npm 패키지로 자체 호스팅, 외부 CDN 의존 없음), 모노스페이스는 시스템 스택(ui-monospace, Menlo, Consolas)
- **컬러**: `client/src/styles/global.css`의 CSS 변수만 사용
  - `--color-navy`(딥 네이비 블루, 상단바/CTA/활성 탭), `--color-navy-light`(강조 배지·보조 액센트), `--color-bg`(화이트), `--color-bg-alt`(옅은 그레이/하늘색, 섹션 구분용), `--color-text`, `--color-border`
  - 새 색이 필요하면 컴포넌트에 hex를 쓰지 말고 `global.css`의 `:root`에 변수를 먼저 추가할 것
- **간격**: 8px 배수 리듬
- **모서리/그림자**: 카드는 옅은 보더 위주, 그림자는 아주 은은하게(진한 드롭섀도우 금지). 탭바 활성 아이콘 뒤 pill 하이라이트 정도만 둥글게, 과도한 라운딩 금지
- **레이아웃 패턴**: 리스트는 카드형, 하단 탭바 고정(아이콘+라벨, 활성 시 네이비 강조), placeholder("준비 중") 화면은 배민 빈 상태(empty state) 패턴처럼 단순 라인 아이콘 + 안내 문구로 구성
- **톤 통일**: 화면 하나에 톤 1개만 유지. 섹션마다 다른 스타일 언어를 쓰지 말 것
- **화면 기준**: 모바일(스마트폰 비율) 우선, 반응형이되 최대 너비 컨테이너로 스마트폰 비율 고정
- **트랜지션**: 화면 전환 시 부드러운 fade/slide 트랜지션 (무거운 애니메이션 라이브러리 없이 CSS transition으로 처리)

## 작업 후
UI/스타일을 변경했다면, chrome-devtools MCP(설치되어 있다면)로 변경된 화면을 스크린샷 찍어 위 금지 목록을 위반하지 않았는지 직접 확인할 것.
